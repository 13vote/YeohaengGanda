package view;

import javax.swing.*;
import java.awt.*;


public class RegionPanel extends JPanel {

    public RegionPanel() {
        setBackground(Color.BLUE);
        add(new JButton("RegionPanel"));
        setVisible(true);
        
        
    }

}
